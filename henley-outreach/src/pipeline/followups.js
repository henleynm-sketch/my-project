// Phase-1 placeholder: follow-up checker.
// Real implementation lands once outreach_attempts has follow_up_date entries.
export async function checkDue() {
  console.log('followups.checkDue() — no due items yet (Phase 2 work).');
  return { due: 0 };
}
